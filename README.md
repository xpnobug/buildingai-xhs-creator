# BuildingAI XHS Creator 小红书图文生成插件

一款 AI 驱动的小红书图文内容生成插件，可根据主题自动生成精美的小红书风格图文内容。

## ✨ 功能特性

- 🎯 **智能大纲生成** - 输入主题自动生成 6-12 页结构化大纲
- 🎨 **批量图片生成** - 支持顺序/并发两种生成模式
- 📊 **实时进度追踪** - SSE 流式推送生成进度
- 🔄 **图片版本管理** - 支持重绘、版本历史、版本恢复
- 💰 **积分计费系统** - 按页面类型扣费，失败自动回退
- 🎁 **免费次数配置** - 支持后台配置每用户免费使用次数
- 📦 **批量下载** - 支持 ZIP 打包下载所有图片

## 🏗️ 技术架构

```
src/
├── api/                          # 后端 (NestJS)
│   ├── db/entities/              # 数据库实体
│   └── modules/xhs-creator/
│       ├── controllers/          # 控制器
│       ├── services/             # 服务层
│       │   ├── image.service.ts           # 图片生成（核心）
│       │   ├── image-billing.service.ts   # 积分计费
│       │   ├── image-version.service.ts   # 版本管理
│       │   ├── generator-resolver.service.ts  # 生成器解析
│       │   └── outline.service.ts         # 大纲生成
│       ├── generators/           # AI 生成器适配
│       └── prompts/              # 提示词模板
└── web/                          # 前端 (Nuxt 3 + Vue 3)
    ├── pages/                    # 页面
    ├── components/xhs/           # 组件
    ├── stores/                   # Pinia 状态管理
    └── services/                 # API 调用
```

## 🚀 快速开始

```bash
# 安装依赖
pnpm install

# 开发模式
pnpm dev

# 构建发布
pnpm build:publish
```

## ⚙️ 配置说明

在后台管理页面配置：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 文本模型 | 大纲生成使用的 AI 模型 | gpt-4o-mini |
| 图片模型 | 图片生成使用的 AI 模型 | gpt-image-1 |
| 大纲积分 | 生成大纲消耗的积分 | 10 |
| 封面积分 | 生成封面页消耗的积分 | 80 |
| 内容积分 | 生成内容页消耗的积分 | 40 |
| 免费次数 | 每用户免费使用次数（大纲+图片共用） | 5 |
| 高并发模式 | 是否启用并行生成 | false |

## 📁 提示词模板

位于 `src/api/modules/xhs-creator/prompts/`：

| 文件 | 用途 | 占位符 |
|------|------|--------|
| `outline_prompt.txt` | 大纲生成 | `{topic}` |
| `image_prompt.txt` | 图片生成 | `{page_content}`, `{page_type}`, `{full_outline}`, `{user_topic}` |

## 📡 API 接口

### 前端 Web API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/outline` | 生成大纲 |
| POST | `/images/generate` | 批量生成图片（SSE） |
| POST | `/images/regenerate` | 重新生成单张图片 |
| GET | `/tasks/:id/progress` | 获取任务进度 |
| GET | `/tasks/:id/download-zip` | 下载图片 ZIP |

## 📄 License

MIT
