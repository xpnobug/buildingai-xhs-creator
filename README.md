# buildingai-xhs-creator

一款 AI 驱动的小红书图文内容生成插件，可根据主题自动生成精美的小红书风格图文内容。

## 功能特性

- 🎯 **智能大纲生成** - 输入主题自动生成 6-12 页结构化大纲
- 🎨 **批量图片生成** - 支持顺序/并发两种生成模式
- 📊 **实时进度追踪** - SSE 流式推送生成进度
- 🔄 **图片版本管理** - 支持重绘、版本历史、版本恢复
- 💰 **积分计费系统** - 按页面类型扣费，失败自动回退
- 🎁 **免费次数配置** - 支持后台配置每用户免费使用次数
- 📦 **批量下载** - 支持 ZIP 打包下载所有图片

## 开发环境配置

### 前置要求

- Node.js >= 18
- pnpm >= 8
- MySQL >= 8.0

### 安装依赖

```bash
pnpm install
```

### 环境变量

创建 `.env.local` 文件：

```env
# 数据库配置
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USER=root
DATABASE_PASSWORD=your_password
DATABASE_NAME=buildingai

# AI 服务配置
OPENAI_API_KEY=your_api_key
OPENAI_BASE_URL=https://api.openai.com/v1
```

### 启动开发服务器

```bash
# 启动前端 + 后端
pnpm dev

# 仅前端
pnpm dev:web

# 仅后端
pnpm dev:api
```

### 构建生产版本

```bash
pnpm build
```

## 项目结构

```
src/
├── api/                    # 后端 NestJS
│   ├── db/                 # 数据库实体和迁移
│   ├── modules/            # 功能模块
│   │   └── xhs-creator/    # 核心业务
│   │       ├── controllers/
│   │       ├── services/
│   │       ├── exceptions/
│   │       └── guards/
│   └── types/              # 类型定义
│
├── web/                    # 前端 Nuxt 3
│   ├── components/         # Vue 组件
│   ├── composables/        # 组合式函数
│   ├── stores/             # Pinia 状态
│   └── pages/              # 页面
│
└── docs/                   # 文档
    ├── API.md              # API 文档
    └── 技术架构文档.md
```

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Nuxt 3, Vue 3, Pinia, TypeScript |
| 后端 | NestJS, TypeORM, RxJS |
| 数据库 | MySQL |
| AI | OpenAI API / 自定义模型 |

## 开发规范

- 使用 TypeScript 严格模式
- 遵循 ESLint + Prettier 规范
- 提交前运行 `pnpm lint` 检查
- 单元测试位于 `tests/` 目录

## 更多信息

- [API 文档](./docs/API.md)
- [技术架构](./docs/技术架构文档.md)
- [更新日志](./CHANGELOG.md)
